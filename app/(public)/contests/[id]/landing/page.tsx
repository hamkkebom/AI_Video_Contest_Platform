import ArirangLandingClient from './arirang-landing-client';

/**
 * 공모전 랜딩 페이지
 * - URL의 [id]를 기반으로 접수하기 등 CTA가 해당 공모전으로 연결됨
 */
export default function ContestLandingPage() {
  return <ArirangLandingClient />;
}
