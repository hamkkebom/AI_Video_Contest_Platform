# DECISIONS — 주요 결정 기록 (ADR-lite)

> **마지막 갱신**: 2026-07-30
> **형식**: 결정 하나당 항목 하나. 배경 → 결정 → 결과. 뒤집힌 결정도 지우지 않고 "번복됨"으로 남긴다.
> D-001~D-007은 git 히스토리 분석으로 2026-07-30에 소급 기록함.

---

## D-001 · 목업 우선 개발 (2026-02-17)

- **배경**: 색상/레이아웃/역할 구조에 대한 클라이언트 검증이 필요했다.
- **결정**: 실데이터 없이 42페이지 인터랙티브 목업(v3)을 먼저 완성하고, 색상 시안 3종 중 Color C(Warm Earth)를 확정.
- **결과**: 목업이 그대로 실서비스의 뼈대가 됨. 산출물: [archive/MOCKUP_COMPLETION_SUMMARY.md](archive/MOCKUP_COMPLETION_SUMMARY.md)

## D-002 · 범용 SaaS → 단일 공모전 선집중 피벗 (2026-02-23)

- **배경**: 원 기획은 멀티테넌트 공모전 SaaS + 함께봄 생태계 허브. 그러나 "제1회 꿈꾸는 아리랑 AI 영상 공모전"(헐버트 아리랑 채보 130주년, 상금 1,300만원)이라는 실제 운영 기회가 확정됨.
- **결정**: 범용 플랫폼 완성보다 아리랑 공모전 실운영을 우선한다. 브랜드를 kkumple(꿈플)로 리네임하고 전용 랜딩을 만든다. (커밋 `af3fd8a`)
- **결과**: 3~4월 253커밋이 실운영 준비·안정화에 투입. 코어는 실전 검증됐고, 수익화 축은 전부 동결 (→ D-005). 원 기획으로의 회귀(SaaS화)는 [ROADMAP.md](ROADMAP.md) 후보 C로 남음.

## D-003 · 영상 인프라 Mux → Cloudflare Stream 교체 (2026-02~03)

- **배경**: PRD 제약사항에는 "Next.js 15, Supabase, **Mux** (변경 불가)"로 명시돼 있었다.
- **결정**: Mux 대신 Cloudflare Stream을 채택. (커밋 2026-02-23 "feat: add Supabase auth, Cloudflare Stream integration")
- **결과**: `lib/cloudflare-stream.ts` 중심의 업로드/재생 파이프라인으로 운영. "변경 불가" 제약도 상황에 따라 뒤집힐 수 있음을 보여준 사례 — 제약은 문서가 아니라 근거와 함께 관리해야 한다.

## D-004 · 기획 문서 25종 전량 삭제 (2026-03-05)

- **배경**: 2026-02-19에 작성한 정식 문서 체계(사업/기획/디자인/기술/관리/테스트/운영, HTML 25종)가 D-002 피벗과 D-003 교체로 실물과 빠르게 어긋남.
- **결정**: "obsolete"로 판단해 전량 삭제. (커밋 `85c155c`)
- **결과**: 실물과 어긋난 문서는 없앴지만, **목표·의도까지 함께 유실**됨 — 이후 4개월간 프로젝트의 "왜"는 git 히스토리에만 존재. 2026-07-30 문서 재구축(D-008)의 직접적 원인. 교훈: 문서를 버릴 땐 유효한 의도만이라도 승계처를 만들 것.

## D-005 · 수익화 동결 (2026-03, 운영 중 암묵 결정)

- **배경**: BM 설계(구독 3플랜, 대행 수수료)가 있었으나 아리랑 공모전은 무료 참여 구조였고 운영 안정화가 급선무였다.
- **결정**: 결제 연동을 하지 않고 가격표 페이지에 "서비스 준비 중"을 노출. `pricing_plans`/`DEFAULT_FEATURE_ACCESS` 골격은 유지.
- **결과**: 명시적 재개 트리거 없이 방치되던 것을 [ROADMAP.md](ROADMAP.md)의 "의도적 보류" 표로 공식화함.

## D-006 · 어뷰징 방지 시스템 도입 (2026-03, 계획외)

- **배경**: 실운영에서 좋아요/조회수 조작 리스크가 실제 문제로 대두 (기획 문서에는 없던 요구).
- **결정**: DB 레벨 방어 체계 구축 — 레이트리밋, 부정 행위 자동 플래깅, IP 로그, RPC 기반 좋아요/조회 기록. (마이그레이션 026, 이후 031·033 보강)
- **결과**: 운영 신뢰성 확보. "계획에 없어도 운영이 요구하면 만든다"는 이 시기 개발 방식의 대표 사례.

## D-007 · 다단계 심사 도입 (2026-04, 계획외)

- **배경**: 단일 라운드 채점 구조로는 실제 공모전의 예선→본선 흐름을 담을 수 없었다.
- **결정**: `judging_stages` / `stage_judges` / `submission_stage_results` 3테이블로 다단계 심사 구조 도입. (마이그레이션 036)
- **결과**: 아리랑 공모전 심사를 실제로 소화. 향후 모든 공모전에 재사용 가능한 범용 구조.

## D-008 · 저장소 위생 정리 + 문서 체계 재구축 (2026-07-30)

- **배경**: 개발 재개 시점 점검에서 두 가지 확인 — (1) 저장소에 env 파일이 추적되고 있었음, (2) D-004 이후 기획 문서가 부재해 로드맵 판단이 불가능했음.
- **결정**: env 파일 추적 제거 및 `.gitignore`를 `.env*` 전면 차단 + `*.example` 예외 방식으로 보강 (커밋 `49240c3`). 삭제된 문서에서 유효 내용을 복원해 경량 문서 체계(VISION / ROADMAP / FEATURES / DECISIONS / archive)로 재구축.
- **결과**: 이 문서 체계. 유지 규칙은 각 문서 상단에 명시.

## D-009 · 공개 데이터는 뷰로만 노출 (2026-07-31)

- **배경**: `profiles`·`submissions`의 SELECT 정책이 `USING (true)`여서 클라이언트 번들에 실리는 anon 키만으로 회원 2,384명의 이메일·전화번호(499건)와 출품자 528명의 실명·전화번호를 전부 조회할 수 있었다. `judges`·`bonus_entries`도 같은 상태였다. 근본 원인은 관리자용 조회 함수가 `unstable_cache` 안에 있어 쿠키를 못 쓰고 익명 클라이언트로 동작한 것 — 관리자 화면이 돌아가려면 정책을 열어야 하는 구조였다.
- **결정**: RLS는 행 단위라 컬럼을 가릴 수 없으므로 **민감 컬럼을 뺀 공개 뷰**(`public_profiles`, `public_submissions`)를 만들고 anon의 원본 테이블 SELECT 권한을 회수했다. 익명 경로는 뷰만, 관리자 경로는 세션 클라이언트로 분리했다. (마이그레이션 042·046, 커밋 `d6fdf00`)
- **결과**: 익명 접근 14개 항목 전부 차단, 공개 페이지·관리자 기능은 그대로 동작. **앞으로 공개 화면에 새 데이터를 노출할 때는 원본 테이블을 열지 말고 뷰에 컬럼을 추가하는 방식을 쓴다.**

## D-010 · 증빙 이미지는 서명 URL로만 (2026-07-31)

- **배경**: `proof-images` 버킷이 공개라 참가자가 올린 SNS 인증 스크린샷 574건을 익명으로 열거·다운로드할 수 있었다.
- **결정**: 3단계로 나눠 적용 — 열거 차단(043) → 읽기 경로를 서명 URL로 전환(코드) → 버킷 비공개(044). 서명 URL은 공개 버킷에서도 동작하므로 중간에 화면이 깨지는 구간이 없다.
- **결과**: 익명 열거·서명 발급·직접 다운로드 모두 차단. DB에는 기존 공개 URL 형식을 유지하고 **읽는 시점에 서명**하므로 574건 데이터 마이그레이션이 불필요했다. 수정 폼이 미리보기 URL을 그대로 되저장하는 구조라 `toStoredProofImageUrl`로 저장 직전 정규화하지 않았으면 만료되는 URL이 DB에 쌓였을 것이다.

## D-011 · 비회원 문의 허용 (2026-07-31)

- **배경**: 문의·대행 폼의 제출 버튼이 `// TODO`만 있어 지금까지 들어온 **모든 고객 문의가 유실**됐다(두 테이블 모두 0건). 게다가 `inquiries`엔 관리자 SELECT 정책이 없고 `agency_requests`엔 SELECT 정책 자체가 없어, 접수가 되더라도 관리자가 볼 수 없는 상태였다.
- **결정**: 공모전 특성상 가입 전 문의가 많으므로 **비회원 접수를 허용**하고 IP 기준 레이트리밋(문의 5회/시간, 대행 3회/시간)으로 스팸을 막는다. `inquiries`에 `guest_*` 컬럼과 "회원 ID 또는 게스트 이메일 중 하나는 필수" 제약을 추가했다. (마이그레이션 045, 커밋 `7230209`)
- **결과**: 비회원 접수 → 관리자 게시판 노출까지 실서비스에서 검증. 관리자 화면은 비회원의 회신 이메일·연락처를 함께 보여줘 바로 답변할 수 있다.

## D-012 · 관측·복구 기반 마련 (2026-07-31)

- **배경**: 에러 모니터링이 없어 장애를 사용자 신고로만 인지했다. 실제로 이날 배포 누락을 라이브 API를 수동으로 찔러보고서야 발견했다. DB 백업 설정도 확인되지 않은 상태였다.
- **결정**: Sentry를 도입하되 **DSN 환경변수가 없으면 초기화를 건너뛰는 구조**로 만들어, 계정 준비 전에도 배포가 막히지 않게 했다. 개인정보를 다루는 서비스이므로 이벤트에서 이메일·전화번호·토큰을 마스킹한다. 함께 헬스체크(`/api/health`)와 DB 스냅샷 스크립트(`scripts/backup-db.mjs`)를 추가했다.
- **결과**: DSN만 넣으면 즉시 수집 시작. CSP는 사용자 입력을 렌더링하는 서비스라 필요하지만 잘못 걸면 영상 재생이 조용히 깨지므로 **Report-Only로 먼저 관찰**하고 위반 로그를 모아 승격하기로 했다.

## D-013 · 정보구조(IA) 재설계 — 미로 해소 (2026-08-01)

- **배경**: UX 감사에서 구조적 미로의 원인이 확인됐다 — (1) 내비 어디에서도 도달 불가능한 고아 페이지 7종(수상작 갤러리·고객센터·검색·크리에이터 등), (2) 대시보드 진입 시 글로벌 헤더가 통째로 사라져 공개 영역과 세계가 단절, (3) 갤러리 작품 상세에 로그인 벽(정작 sitemap엔 최대 1,000건 등록), (4) 헤더 "공모전" 기본 필터가 빈 화면 노출, (5) 관리자 사이드바에 "준비중" 메뉴 6개 노출, (6) 홈 대행 CTA가 자체 접수 파이프라인(D-011로 검증됨)을 두고 외부 사이트로 유출.
- **결정**: [IA.md](IA.md)를 정보구조 단일 기준 문서로 신설. 핵심 원칙 — 공개 가치(감상)는 열고 행위(출품·좋아요)에만 로그인 요구, 존재하는 페이지는 반드시 내비에 연결(미완성이면 내비·sitemap 양쪽 제외), 대시보드에서도 글로벌 헤더 유지, 기본 필터는 콘텐츠 있는 탭 자동 선택. 갤러리 상세는 공개로 전환하고, pricing은 동결(D-005) 유지하되 sitemap에서 제외. 대시보드 라우트 통폐합과 landing canonical 정리는 로드맵 후보로 이관.
- **결과**: 2026-08-01 구현 완료. 헤더 4메뉴+검색, 푸터 3컬럼 복원, 갤러리 상세 공개(데이터 방어는 public 뷰+RLS로 기존부터 견고함을 리뷰로 확인), `/invite` 로그인 가드, 관리자 사이드바 5섹션(가짜 "준비중" 라벨 6개 폐기 — 전부 실구현 페이지였음), 공개 상태 라벨 단일 소스화(closed=마감으로 교정). **남긴 트레이드오프**: 익명 조회수가 쌓이므로 조회수를 점수에 반영하는(`online_vote_type`=views 포함) 공모전 운영 전에 인증 조회 집계 정책이 필요하다 (ROADMAP 이관). 현 기본값은 likes라 즉시 영향 없음.

## D-014 · 주최자를 1급 개념으로 노출 + companies 익명 차단 (2026-08-01)

- **배경**: 로드맵 후보 C(멀티테넌트 SaaS) 방향이 정해지면서 주최자를 공개해야 했는데, 코드는 주최자를 단일 테넌트로 가정하고 있었다. 공모전 상세의 주최 표기가 `isAdminHost ? '함께봄 주식회사' : hostUser.name` 으로 **하드코딩**돼 있어 외부 주최자의 공모전도 운영사 이름으로 표시됐고, `contests.host_company_id` 는 어디서도 읽히지 않았다. 더 큰 문제로, **042(D-009)가 `companies`·`company_members` 를 빠뜨려** 두 테이블이 아직 `FOR SELECT USING (true)` + anon SELECT 권한 상태였다. 사업자등록번호·대표자명·주소·연락처·사업자등록증 이미지 URL과 소속 관계가 anon 키로 열려 있었고, 인증 검사가 없는 `/api/admin/companies/[id]` 가 미들웨어 보호 밖(`/api/admin/*` 은 `/admin` 으로 시작하지 않는다)에 있어 그대로 반환됐다. 확인 결과 기업 데이터가 0건이라 실제 유출은 없었다 — 외부 주최자를 받기 직전이 마지막 차단 시점이었다.
- **결정**: 042와 같은 패턴으로 `public_companies` 뷰(id·name·logo_url·website·description, `status='approved'` 한정)를 만들고 원본 테이블의 anon SELECT 를 회수했다. `company_members` 는 본인·같은 회사·관리자만 조회한다(자기 참조 재귀를 피하려 `is_company_member()` SECURITY DEFINER 함수 사용). 주최 표기는 **등록 기업 → 플랫폼 자체 개최(admin) → 주최 담당자** 순으로 해석하고, 공개 주최자 페이지 `/hosts/[id]` 를 신설했다. Event JSON-LD 의 organizer 도 플랫폼 고정에서 실제 주최자로 바꿨다. (마이그레이션 048)
- **결과**: 주최자가 공개 화면에 존재하게 됐고 사업자 정보는 뷰 밖으로 나갈 수 없다. 부수 발견 — `/hosts` 도입으로 `pathname.startsWith('/host')` 가 공개 주최자 페이지까지 로그인으로 보내는 접두사 충돌이 드러나, 미들웨어·헤더·푸터의 경로 비교를 세그먼트 경계(`isUnder`)로 교정했다. 인증 없는 관리자 API 는 `/api/admin/companies/[id]` 하나만 실제 구멍이었고(나머지 4개는 RLS 가 쓰기를 막고 있었다) 명시적 관리자 확인을 추가했다.

## D-015 · 주최자 온보딩은 좁은 RPC 두 개로 (2026-08-01)

- **배경**: 멀티테넌트(D-014)의 다음 조각은 외부 주최자가 스스로 기업을 등록하는 흐름인데, RLS 구조가 셀프서비스를 막고 있었다 — ① `company_members`에 INSERT 정책이 없고, 열면 아무나 임의 회사의 멤버로 자신을 추가할 수 있다(소속 위조). ② 신청 직후에는 아직 멤버가 아니라 companies의 "소속 멤버 조회" 정책에 걸려 `INSERT ... RETURNING`도 실패한다(가입 전엔 자기 회사를 SELECT할 수 없는 순환). ③ profiles는 "본인만 수정"이라 관리자가 승인 시점에 host 역할을 부여할 방법이 없다.
- **결정**: 테이블 쓰기 권한을 여는 대신 검증이 내장된 SECURITY DEFINER RPC 두 개만 연다 — `apply_host_company`(기업 pending + 신청자 owner 멤버십 원자 생성, 중복 신청 차단)와 `admin_set_company_status`(is_admin 강제, 승인 시 owner 전원에게 host 역할 부여). 직접 INSERT 정책은 `status='pending'` 강제로 교체해 우회 승인을 막았다. (마이그레이션 049) 흐름: `/hosts/apply`(로그인 필요) → `/admin/companies` 승인/반려 버튼 → host 대시보드 개방. 진입점은 푸터 "공모전 개최 신청".
- **결과**: 온보딩 코드 완성, RPC 미배포 환경에선 503으로 우아하게 실패. **048·049 마이그레이션은 MCP가 읽기 전용이라 수동 적용 필요** — 적용 전까지 신청 접수 불가.

---

## 새 결정 추가 방법

```markdown
## D-XXX · 제목 (YYYY-MM-DD)

- **배경**: 왜 결정이 필요했나
- **결정**: 무엇을 하기로 했나 (관련 커밋/PR)
- **결과**: 실제로 어떻게 됐나 (나중에 채워도 됨)
```
