# ROADMAP — 살아있는 로드맵

> **마지막 갱신**: 2026-08-02
> **갱신 규칙**: 기능의 상태가 바뀌면(착수/완료/보류/폐기) 이 문서를 같은 커밋에서 함께 갱신한다. 분기마다 한 번 "보류" 항목의 재개 트리거를 재평가한다.

## 현재 위치 (2026-07-30 기준)

- ✅ "제1회 꿈꾸는 아리랑 AI 영상 공모전" **접수→심사→발표 1사이클 완주** (2026-03~04)
- 이후 유지보수 모드 (2026-05~07 기능 커밋 없음)
- ✅ 2026-08-01 **정보구조(IA) 재설계** — 미로 구조 해소: 내비 재구축(수상작·고객센터·검색·크리에이터 연결), 갤러리 상세 공개 전환, 대시보드에서 글로벌 헤더 유지, 심사위원 초대의 역할 가드 차단 해소. 기준 문서 [IA.md](IA.md), 결정 [DECISIONS.md](DECISIONS.md) D-013
- ✅ 2026-08-01 **멀티테넌트 1차** — 갤러리 공모전 스코프, 주최자 공개 페이지(`/hosts/[id]`)·주최자 표기, 홈 N개 공모전 그리드, 주최자 온보딩(`/hosts/apply`→승인→host 부여). companies 익명 노출 차단(D-014). **⚠️ 마이그레이션 048·049 수동 적용 필요** (D-015)
- **다음 마일스톤 미결정** — 아래 [후보](#다음-마일스톤-후보) 중 선택 필요

### 남은 과제 — 우선순위 (2026-08-02 정리)

> 기준은 "다음 마일스톤을 막는가". 방향은 후보 C(멀티테넌트 SaaS)로 정해졌고,
> 실제로 외부 주최자를 받고 제2회를 여는 것이 다음 단계다.

| # | 과제 | 왜 이 순서인가 | 선행 조건 |
|:-:|------|----------------|-----------|
| ~~**1**~~ | ~~**이메일 발송**~~ | ↓ 아래 1-1 로 대체. 필요했던 세 가지 중 **회원 문의 답변·주최자 승인 통지는 계정이 있는 사람에게 가는 통지**였다 — 이메일 없이 사이트 안에서 닫았다 (D-017). **⚠️ 마이그레이션 053 수동 적용 필요** |
| ~~**1-1**~~ | ~~**비회원 문의 회신**~~ | ✅ 2026-08-03 완료 — Resend 도메인 검증(`aikkumhub.com`, 도쿄) + `RESEND_API_KEY` 설정 + 배포까지 끝. 답변 저장 시 자동 발송 |
| ~~**2**~~ | ~~**심사위원 초대 실동작화**~~ | ✅ 2026-08-02 완료 — 초대가 아니라 **배정**으로 설계 변경(D-018). 발견된 것이 목업만이 아니었다: `judges` 에 쓰기 RLS 정책이 없어 배정 자체가 DB 에서 막혀 있었고, 주최자가 남의 공모전에도 심사위원을 넣을 수 있었다. **⚠️ 마이그레이션 054 수동 적용 필요** |
| ~~**2-1**~~ | ~~**외부 인사 심사위원**~~ | ✅ 2026-08-03 완료 — 미가입 주소에 초대 메일 발송, `/judge-invite/[token]` 수락(D-022). 토큰은 초대받은 이메일에 묶이고 14일 만료. **⚠️ 마이그레이션 055 수동 적용 필요** |
| ~~**3**~~ | ~~**관리자 편집 폼**~~ | ✅ 2026-08-02 완료 — 아티클 수정 폼(`/admin/articles/[id]/edit`)과 회원 역할 편집. **⚠️ 마이그레이션 052 수동 적용 필요** (역할 변경 RPC) |
| **4** | **익명 조회수 점수 정책** | 조건부. `online_vote_type`에 views를 포함한 공모전을 **열기 전에** 인증 조회 집계 정책이 필요하다. 현 기본값이 likes라 지금은 무해 | 제품 결정 |
| **5** | **제출 메가폼 분해** | 🔸 부분 완료 (2026-08-02) — 업로드 로직과 결과 다이얼로그를 분리해 2,235→1,688줄. **복붙 때문에 재제출만 실패하던 문제를 같이 고쳤다**(D-019). 남은 것은 폼 STEP JSX 약 600줄 | — |
| **6** | **아리랑 랜딩 공모전별 분리** | `/contests/[id]/landing`이 모든 공모전에 아리랑 전용 컴포넌트를 렌더한다(`id === '3'` 분기까지 있다). 제2회 랜딩이 필요해지면 그때 터진다 | 제2회 확정 |
| ~~**7**~~ | ~~**대시보드 라우트 통폐합**~~ | ✅ 2026-08-02 완료 — 라우트를 옮기는 대신 사이드바끼리 이었다(D-020). 부수로 **관리자가 주최자 대시보드에서 홈으로 튕기던 죽은 링크**를 고쳤다 |
| **8** | **관리자 공모전 폼 분해** | 🔸 부분 완료 (2026-08-02) — 폼 타입·초기값 팩토리·포맷·업로드·영상 썸네일 추출을 `contest-form-helpers.ts` 로 분리(2,463→2,166줄). 남은 것은 카드 10개 JSX 약 1,200줄 | — |
| **9** | **pricing 가짜 가격 폴백 제거** | 수익화가 동결(D-005)이라 착수 조건이 아직 없다. 현재 noindex + sitemap 제외로 노출은 막아둠 | D-005 해제 |

기존 항목 이력:

| 항목 | 내용 |
|------|------|
| ~~대시보드 라우트 통폐합~~ | ✅ 2026-08-02 완료 — 라우트 이동 없이 사이드바에 "다른 대시보드" 섹션을 붙이는 방식으로 해결 (D-020) |
| ~~landing/상세 canonical 정리~~ | ✅ 2026-08-01 완료 — landing 의 canonical 을 상세로 지정하고 sitemap 에서 제외. 아리랑 전용 랜딩의 공모전별 분리는 별도 과제로 남음 |
| ~~심사위원 초대 실동작화~~ | ✅ 2026-08-02 완료 — 회원 직접 배정으로 전환, `/invite/[token]` 목업 삭제. **⚠️ 마이그레이션 054 수동 적용 필요** |
| ~~문의 답변 경로 부재~~ | ✅ 2026-08-02 완료 — `inquiries.answer` 신설, 관리자 답변 폼 + 회원 `/my/inquiries` 열람. **⚠️ 마이그레이션 053 수동 적용 필요**. 비회원 문의는 여전히 밖으로 회신해야 한다 |
| pricing 가짜 가격 폴백 제거 | 수익화 재개(D-005) 시점에 처리 |
| 익명 조회수 점수 정책 | 갤러리 공개 전환으로 비로그인 조회가 쌓임 — `online_vote_type`에 views 포함 공모전 운영 전 인증 조회 집계 정책 필요 (현 기본값 likes라 즉시 영향 없음) |
| ~~sitemap·상세의 draft 공모전 배출~~ | ✅ 2026-08-01 완료 — sitemap 쿼리에 `.neq('status','draft')`, 상세·메타데이터에 draft 가드(noindex). 수상작 URL 도 결과 나온 공모전만 배출 |
| ~~동적 상세 페이지 소프트 404~~ | ✅ 2026-08-01 완료 — 원인이 둘이었다: ① 세 페이지가 `notFound()` 대신 커스텀 UI를 200으로 렌더 ② loading.tsx 가 스트리밍을 먼저 시작. 공용 `(public)/not-found.tsx` 신설 + 상세 라우트 loading 제거 + story 의 try/catch 가 삼키던 예외를 `unstable_rethrow` 로 통과. 4개 라우트 404 확인 |
| 제출 메가폼·관리자 공모전 폼 분해 | 🔸 부분 완료 — 두 파일 모두 순수 로직을 분리했다(D-019). 남은 것은 폼 JSX 뿐 |
| ~~관리자 죽은 컨트롤~~ | ✅ 2026-08-02 완료 — 필터 4종, 문의·대행 상태 변경, 아티클 발행 토글·삭제, 회원 정지를 실동작화. **⚠️ 마이그레이션 050 수동 적용 필요** (articles 쓰기 정책이 아예 없어 아티클 생성조차 RLS 에 막히고 있었다) |
| ~~관리자 편집 폼~~ | ✅ 2026-08-02 완료 — 아티클 수정 폼 신설, 회원 역할 편집(participant/host/judge). admin 승격은 의도적으로 UI 경로에서 제외 |
| ~~갤러리 공모전별 필터~~ | ✅ 2026-08-01 완료 — `?contest=[id]` 필터가 기간 필터 6버튼을 대체 |

---

## ✅ 운영 검증 완료 (Phase 1 + 계획외 확장)

원 기획의 Phase 1 전체와, 실운영 과정에서 추가된 기능들. 상세 목록·코드 위치는 [FEATURES.md](FEATURES.md).

| 영역 | 내용 |
|------|------|
| 공모전 코어 | 생성/관리, 출품 접수(영상 업로드), 심사(단일+다단계), 결과 발표, 갤러리, 좋아요 |
| 인증/계정 | Supabase Auth (이메일 + Google OAuth), 역할 기반 접근 제어, 세션 슬라이딩 만료, 기기 관리, 회원 탈퇴 |
| 영상 인프라 | Cloudflare Stream 업로드/재생 파이프라인 (기획의 Mux에서 교체 — [DECISIONS.md](DECISIONS.md) D-003) |
| 운영 도구 | 관리자 대시보드, 대리 출품 등록, 팝업 관리, 사이트 설정, 활동 로그, UTM 추적 |
| 신뢰/보안 | 어뷰징 방지(레이트리밋, 부정 좋아요/조회 감지, IP 로그), 가산점 인증 승인 워크플로 |
| 콘텐츠 | 통합 검색, 소식/아티클, FAQ/문의, 아리랑 전용 마이크로사이트 |

## ⏸️ 의도적 보류 (재개 트리거와 함께)

> 보류는 실패가 아니라 결정이다. 트리거 조건이 충족되기 전에는 착수하지 않는다.

| 항목 | 원 계획 | 현재 상태 | 재개 트리거 (제안) |
|------|---------|-----------|--------------------|
| 결제 연동 | Phase 1 후속 (Toss Payments) | SDK 미설치, 코드 없음 | 유료 기능이 하나라도 확정될 때 (구독 또는 대행 수수료) |
| 구독 요금제 | Phase 2 — 3플랜 과금 | `pricing_plans` 테이블 + 가격표 UI("서비스 준비 중")만 존재. `PaywallOverlay` 컴포넌트 미사용 | 월간 활성 사용자/주최 문의가 유의미해질 때 |
| ~~**이메일 발송**~~ | — | ✅ 2026-08-02 코드 도입 (`lib/email`, Resend REST). 회원 통지는 내부 문의함이 처리하므로 **계정 없는 상대에게만** 쓴다(D-021). DNS는 이미 등록돼 있었다 — 로드맵의 "DNS 레코드 필요" 서술이 틀렸다 | ⚠️ `RESEND_API_KEY` 미설정 — 넣기 전까지 실제 발송 안 됨 |
| 대행 플랫폼 연계 | Phase 2 — 연결 수수료 | 대행 의뢰 **접수는 동작**(2026-07-31), 이후 견적·매칭 프로세스 없음 | 대행 플랫폼(별도 레포) 실체가 생길 때 |
| 프리랜서 신청 시스템 | Phase 2 | 미구현 (문구만 존재) | 대행 연계와 동시 검토 |
| AI 심사 자동화 | Phase 2 (구 F-009) | 미구현. 어뷰징 자동 플래깅은 별도로 구현됨 | 제2회 이상 공모전에서 심사 병목이 확인될 때 |
| 검색 자동완성 | Phase 2 | 통합 검색은 구현, 자동완성 없음 | 검색 사용량 데이터 확보 후 |
| 함께봄 통합 SSO | Phase 2 | Google OAuth만 구현. 생태계 SSO는 미착수 | 생태계 두 번째 서비스가 생길 때 |
| 분석 기능 구독 | Phase 3 | `DEFAULT_FEATURE_ACCESS` 접근 매트릭스만 존재 | 구독 요금제와 동시 |
| 인증 배지 시스템 | Phase 3 | 미구현 | 크리에이터 생태계 활성화 후 |

## ❌ 폐기 후보 (결정 대기)

> 아직 공식 폐기된 것은 없다. 다음 로드맵 결정 시 유지/폐기를 명시할 것.

| 항목 | 근거 |
|------|------|
| 교육 플랫폼 연계 | 기획 이후 어떤 커밋에서도 언급 없음 |
| i18n (한/영) | next-intl 설치·준비만 되고 실사용 없음 — 유지 시 비용, 폐기 시 의존성 제거 가능 |
| `dev` 브랜치 | main보다 280커밋 뒤처진 방치 브랜치 — 삭제 권장 |

## 다음 마일스톤 후보

셋 중 하나(또는 순서)를 결정해야 로드맵이 다시 움직인다:

| 후보 | 내용 | 선행 조건 |
|------|------|-----------|
| **A. 제2회 공모전 운영** | 검증된 코어로 다음 공모전 개최. 가장 리스크 낮음 | 공모전 기획(외부), 운영 인력 |
| **B. 수익화 착수** | 결제 연동 + 구독 or 대행 수수료 활성화 | 과금 대상 기능 확정, PG 계약 |
| **C. 멀티테넌트 SaaS화** | 외부 주최자에게 개방 (원래 기획으로 회귀) | 온보딩/과금/지원 체계, B 선행 권장 |

---

## 이력

- **2026-02-17**: 목업 v3 완성 (42페이지) — [archive/MOCKUP_COMPLETION_SUMMARY.md](archive/MOCKUP_COMPLETION_SUMMARY.md)
- **2026-02-19**: 정식 기획 문서 25종 작성 (이후 3-05 삭제, D-004)
- **2026-02-23**: 실서비스 피벗 — 아리랑 공모전 선집중 (D-002)
- **2026-03-03~05**: Supabase 실데이터 전환 + Cloudflare Stream 통합
- **2026-03~04**: 아리랑 공모전 실운영 (392커밋 중 253커밋이 이 기간)
- **2026-05-22**: egress 최적화를 끝으로 유지보수 모드 진입
- **2026-07-30**: 저장소 위생 정리(env 추적 제거) + 문서 체계 재구축 (D-008)
